# Caddy issues and renews a real Let's Encrypt certificate for this domain
# automatically the first time it starts -- no manual certbot/nginx config.
meetingsaathi.apriltech.org {
	reverse_proxy app:8420
}
